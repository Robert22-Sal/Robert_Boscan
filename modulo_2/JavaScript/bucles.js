// ejercicio 1
const multiplo = 2;


for (let i = 1; i <= 10; i++) {
    
    const resultado = multiplo * i;
    console.log(`${multiplo} x ${i} = ${resultado}`);
}

// y hasta aqui llegue porque recuerde lo que me paso...